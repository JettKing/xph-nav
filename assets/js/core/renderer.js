/** XPH Resource Renderer V5.3: renders only validated Contract resources. */
window.ResourceRenderer={
  render({container="#resource-list",data=[]}={}){const el=document.querySelector(container);if(!el)return;el.innerHTML=data.length?data.map(ResourceTemplates.card.bind(ResourceTemplates)).join(""):ResourceTemplates.empty("没有找到符合条件的资源");this.updateCount(ResourceStore.getFilteredCount());this.renderPagination();this.refresh();},
  updateCount(count){const el=document.getElementById("resourceCount");if(el)el.textContent=`${Number(count)||0} 个资源`;},
  renderPagination(){let wrap=document.querySelector("[data-pagination]");if(!wrap){wrap=document.createElement("div");wrap.dataset.pagination="true";wrap.className="v5-pagination";document.querySelector("#resource-list")?.insertAdjacentElement("afterend",wrap);}const pages=ResourceStore.getPageCount(),current=ResourceStore.getState().page;if(pages<=1){wrap.innerHTML="";return;}const buttons=[];for(let i=1;i<=pages;i++){if(i===1||i===pages||Math.abs(i-current)<=1)buttons.push(`<button type="button" class="category-btn ${i===current?"active":""}" data-page="${i}">${i}</button>`);else if(buttons[buttons.length-1]!=="…")buttons.push("…");}wrap.innerHTML=`<button type="button" class="category-btn" data-page="${Math.max(1,current-1)}" ${current===1?"disabled":""}>上一页</button>${buttons.map(v=>v==="…"?`<span class="v5-page-gap">…</span>`:v).join("")}<button type="button" class="category-btn" data-page="${Math.min(pages,current+1)}" ${current===pages?"disabled":""}>下一页</button>`;},
  refresh(){window.ResourceAppRefresh?.();},
  loading(container="#resource-list"){const el=document.querySelector(container);if(el)el.innerHTML=ResourceTemplates.loading();},
  empty(container="#resource-list",message="暂无资源"){const el=document.querySelector(container);if(el)el.innerHTML=ResourceTemplates.empty(message);this.updateCount(0);},
  clear(container){if(container)container.innerHTML="";}
};